# Postgres connection string (Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/ai_writer?schema=public"

# OpenAI
OPENAI_API_KEY="sk-..."

# Optional: your own y-webrtc signaling server(s), comma separated.
# Defaults to public demo signaling servers if unset (fine for local dev, not for production).
NEXT_PUBLIC_SIGNALING_SERVERS=""
